
  # Oxent x SportsprintMx

  This is a code bundle for Oxent x SportsprintMx. The original project is available at https://www.figma.com/design/8cxb8GVFeVz4cWxxC7hVsa/Oxent-x-SportsprintMx.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  